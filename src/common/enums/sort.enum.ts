export enum SortOrder {
    ASC = 'ASC',
    DESC = 'DESC',
}

export enum BusinessSortBy {
    CreatedAt = 'createdAt',
    UpdatedAt = 'updatedAt',
    Name = 'name'
}

export enum MarketingSourceSortBy {
    CreatedAt = 'createdAt',
    UpdatedAt = 'updatedAt',
    Name = 'name',
    Channel = 'channel',
    CampaignName = 'campaignName',
}

export enum TrackingNumberSortBy {
    CreatedAt = 'createdAt',
    UpdatedAt = 'updatedAt'
}