export enum NumberRouteStatus {
    Active = 'active',
    Suspended = 'suspended',
    Deleted = 'deleted',
}

export enum TrackingNumberStatus {
    Active = 'active',
    Suspended = 'suspended',
    Releasing = 'releasing',
    Released = 'released',
}

